$(document).ready(function() {
	var smallbox = index of ".boxsize"
	$("#submitme").click(function(){
		$(index  of ".boxsize").css("background-postion", top: 0+=75, left: 0);
		//the first 8 divs just move over 75px
		//the divs 9-48 move +75 once on Y and then moves 75 across 8 tim
		run 8 times then;

	});
};